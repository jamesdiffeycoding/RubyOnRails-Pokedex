<%= form_with(model: pokemonster) do |form| %>
  <% if pokemonster.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pokemonster.errors.count, "error") %> prohibited this pokemonster from being saved:</h2>

      <ul>
        <% pokemonster.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :pokemon_ID, style: "display: block" %>
    <%= form.number_field :pokemon_ID %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
